
function performOperation(si, sd, ss) {
    const fi = 4;
    const fd = 4.0;
    const fs = 'HackerRank ';
    const sumInteger = fi + parseInt(si);
    console.log(sumInteger);
    const sumDecimal = fd + parseFloat(sd);
    console.log(sumDecimal);
    const sumString = fs + ss;
    console.log(sumString);
}
